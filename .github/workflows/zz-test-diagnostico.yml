name: zz-test-diagnostico

on:
  push:
    branches: [main]
    paths:
      - ".github/HEARTBEAT.md"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
